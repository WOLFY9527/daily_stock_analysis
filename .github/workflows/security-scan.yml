name: Security Scan

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/security-scan.yml'
      - 'api/**'
      - 'bot/**'
      - 'data_provider/**'
      - 'docker/**'
      - '*.py'
      - 'patch/**'
      - 'src/**'
      - 'strategies/**'
      - 'scripts/security_scan.sh'
      - 'requirements*.txt'
      - 'pyproject.toml'
      - 'apps/dsa-web/package.json'
      - 'apps/dsa-web/package-lock.json'
  push:
    branches: [main]
    paths:
      - '.github/workflows/security-scan.yml'
      - 'api/**'
      - 'bot/**'
      - 'data_provider/**'
      - 'docker/**'
      - '*.py'
      - 'patch/**'
      - 'src/**'
      - 'strategies/**'
      - 'scripts/security_scan.sh'
      - 'requirements*.txt'
      - 'pyproject.toml'
      - 'apps/dsa-web/package.json'
      - 'apps/dsa-web/package-lock.json'
  workflow_dispatch:

concurrency:
  group: security-scan-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  secret-scan:
    name: secret-scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Gitleaks scan with redacted output
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: >-
            ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_ENABLE_COMMENTS: 'false'
        with:
          args: detect --source . --redact --no-banner --exit-code 1

  python-dependency-audit:
    name: python-dependency-audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install audit tool
        run: python -m pip install pip-audit

      - name: Audit Python requirements
        run: python -m pip_audit -r requirements.txt -r requirements-dev.txt

  frontend-npm-audit:
    name: frontend-npm-audit
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/dsa-web
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/dsa-web/package-lock.json

      - name: Audit production frontend dependencies
        run: npm audit --omit=dev --audit-level=high

  sast-static-checks:
    name: sast-static-checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install SAST tool
        run: python -m pip install 'bandit[toml]'

      - name: Run Bandit SAST
        run: bandit -c pyproject.toml -r api bot data_provider src main.py server.py

  container-vulnerability-scan:
    name: container-vulnerability-scan
    runs-on: ubuntu-latest
    needs: [secret-scan, python-dependency-audit, frontend-npm-audit, sast-static-checks]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Prepare verified Web package
        run: |
          set -euo pipefail
          ./wolfy bootstrap --ensure
          candidate_sha="$(git rev-parse HEAD)"
          candidate_tree="$(git rev-parse HEAD^{tree})"
          ./wolfy exec --profile test -- python scripts/docker_package.py context \
            --output output/docker-context --expected-sha "${candidate_sha}"
          artifact_fingerprint="$(python -c 'import json; print(json.load(open("output/docker-context/.wolfystock-package-identity.json", encoding="utf-8"))["artifact"]["fingerprint"])')"
          echo "WOLFYSTOCK_CANDIDATE_SHA=${candidate_sha}" >> "${GITHUB_ENV}"
          echo "WOLFYSTOCK_CANDIDATE_TREE=${candidate_tree}" >> "${GITHUB_ENV}"
          echo "WOLFYSTOCK_ARTIFACT_FINGERPRINT=${artifact_fingerprint}" >> "${GITHUB_ENV}"

      - name: Build local scan image
        run: |
          docker build \
            --build-arg WOLFYSTOCK_CANDIDATE_SHA="${WOLFYSTOCK_CANDIDATE_SHA}" \
            --build-arg WOLFYSTOCK_CANDIDATE_TREE="${WOLFYSTOCK_CANDIDATE_TREE}" \
            --build-arg WOLFYSTOCK_ARTIFACT_FINGERPRINT="${WOLFYSTOCK_ARTIFACT_FINGERPRINT}" \
            -t daily-stock-analysis:security-scan -f output/docker-context/docker/Dockerfile output/docker-context

      - name: Scan local image with Trivy
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: daily-stock-analysis:security-scan
          format: table
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: '1'
          scanners: vuln
